#!/bin/bash
git config -f .gitmodules submodule.source.branch master
git submodule init
git submodule update --remote
cp -rf ./source/dist/* ./
git config -f .gitmodules submodule.source.branch master